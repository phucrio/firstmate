## Evidence key

- **[R]** independently verified from files under `/home/ubuntu/Desktop/firstmate`.
- **[M]** current process/forge fact supplied by the main supervisor.
- Status files are historical receipts, not current liveness evidence.

## 1. Active continuation

| Task | Authorized deliverable | Evidence and remaining work | Safest supervisor action |
|---|---|---|---|
| `diagnose-nextzero-cli-v113-beta1-docker-login-r1` | State-aware fresh Docker enrollment fix that preserves enrolled-node reconnect behavior; tests and PR to `stg`. | **[R]** Diagnosis: `data/diagnose-nextzero-cli-v113-beta1-docker-login-r1/report.md`. Historical implementation/test progress, including commit `2098801`: `state/diagnose-nextzero-cli-v113-beta1-docker-login-r1.status`. **[M]** Sole live worker, recently relaunched in its original isolated copy. Final head, pipeline, and PR are not yet stable. | **Do not inspect, relaunch, or modify its worktree.** Let it continue and await its next durable terminal report. |

## 2. Restart/recovery-needed implementation

| Task | Authorized deliverable | Evidence and remaining work | Safest supervisor action |
|---|---|---|---|
| `dlp-browser-wire-contract-f1` | Endpoint-owned strict Chrome-first wire schema, generated Rust/TypeScript bindings, all 19 vectors, and bounded simulated host/PEP harness; no real-browser qualification or merge. | **[R]** Scope: `data/dlp-browser-wire-contract-f1/brief.md`. Historical implementation and validation: `state/dlp-browser-wire-contract-f1.status`. Latest finding: `data/dlp-browser-wire-contract-f1/nm-01M2JH57W97ZTWYQD9D5R5R4W1-fix24-findings.txt`. Local submitted head `06136785e02578a63ca144b1dcbb3c9ee097fec4`. **[M]** Run `01M2JH57W97ZTWYQD9D5R5R4W1` failed only because the daemon stopped; unpublished pipeline head is `704165985b38c3a268cc97e5dfe3800783051c6c`. Windows private-directory/ACL finding may remain unresolved. | **Priority recovery:** in the exact worktree recorded by `state/dlp-browser-wire-contract-f1.meta`, run exactly `no-mistakes axi sync --recover` before editing, resetting, or rebasing. Preserve both heads and follow the returned custody action. If the Windows finding remains open, obtain an explicit answer and continue the same guarded run to one green PR. Do not claim Windows or real-browser qualification and do not merge. |

## 3. Green PRs awaiting approval

| Task | Authorized deliverable | Evidence and remaining work | Safest supervisor action |
|---|---|---|---|
| `nextzero-pkgs-telegram-release-alert-r1` | Admin-triggered Telegram release alert with configurable bilingual template, eligibility/confirmation controls, durable audit/idempotency/recovery, and UI tests; no automatic or real Telegram send. | **[R]** Scope: `data/nextzero-pkgs-telegram-release-alert-r1/brief.md`. Historical final head `2eab03b739a255536c214a22b1e7bec38395150c`, base `stg`, resolved threads, responsive UI evidence, stale-send recovery, and no Telegram contact: `state/nextzero-pkgs-telegram-release-alert-r1.status`. **[M]** PR [256](https://github.com/nextzeroinc/nextzero-pkgs/pull/256) is open with 2 passed, 0 failed, 2 skipped. Contradiction: historical status calls run `01M2MZXGVJ5THSQN256NJK6BFH` successful, while authoritative state says it failed when the daemon stopped. The live PR is nevertheless green. | Treat PR 256 at `2eab03b…` as the shipping object. Verify base `stg`, current review/thread state, expected skipped Docker/deploy jobs, accepted template/hashtag behavior, stale-send recovery, stored-secret readiness, and no-real-send boundary. Check any visual-review follow-up issue only if the brief makes it required. If scope is satisfied, present the existing PR for captain approval; otherwise make only the missing in-scope correction on the same PR. **Do not create another audit gate or contact Telegram.** |
| `fix-nextzeroctl-pr809-codex-r1` | Resolve five Codex findings on existing PR 809 while preserving release-merge semantics. | **[R]** Head `d5a537e0baaefc53bb0f66fbfdb92813a79727eb`, five resolved threads, and five green checks: `state/fix-nextzeroctl-pr809-codex-r1.status`. **[M]** PR [809](https://github.com/nextzeroinc/nextzeroctl/pull/809) remains open with five passing checks. Merging triggers Enterprise deployment/release automation; squash/rebase would bypass the intended detector. | Keep it open and unchanged. Obtain **fresh explicit captain merge confirmation**. If granted, use only the documented merge-commit flow; otherwise do nothing. |

## 4. Ownership-blocked implementation

| Task | Authorized deliverable | Evidence and remaining work | Safest supervisor action |
|---|---|---|---|
| `guard-existing-pr-pipeline-r1` | Fail-closed workflow proving an explicitly supplied PR’s repo/base/head/upstream and preventing fallback to a replacement `fm/<task-id>` PR. | **[R]** Scope: `data/guard-existing-pr-pipeline-r1/brief.md`. Audit verifies four preserved commits ending at `059eee4617fd4f5c2b0203cee376a4b9945939a9`, implementation paths, and focused tests: `data/audit-firstmate-backlog-r1/report.md`. Push failure HTTP 403 as `phucrio` to `kunchenguid/firstmate`: `state/guard-existing-pr-pipeline-r1.status`. Contradiction: the brief’s progress note claims fork push was configured, but later status/audit evidence shows delivery remained blocked. Its recorded copy is shared with the audit task. | Scope remains authorized, but delivery ownership is unresolved. Do not use or clean the shared copy. Recover `059eee…` into a fresh isolated Firstmate worktree, reconcile with current main, rerun focused tests, and prove actual write access to the captain-approved fork before starting guarded delivery. If permission still fails, report the access block rather than creating another route. |

## 5. Artifact-uncertain read-only work

| Task | Authorized deliverable | Evidence and remaining work | Safest supervisor action |
|---|---|---|---|
| `review-pr716-stg-r2` | Read-only review of nextzeroctl PR 716 against current `stg`, including the accepted `latest` contract and known risk areas. | **[R]** Only `data/review-pr716-stg-r2/brief.md`, `state/review-pr716-stg-r2.meta`, and an empty `state/review-pr716-stg-r2.turn-ended` remain. No report or status receipt independently proves completion. **[M]** It was classified as a completed scout, creating an evidence contradiction. Its copy is shared with the posture-research task, and any old review may now be stale. | First recover any missing transcript/report. If none exists and the review is still relevant, authorize a fresh read-only review against the current PR/base in a new isolated copy while retaining the original task identity. Do not relaunch or clean the shared copy blindly. |

## 6. Completed reports

| Task | Authorized deliverable | Completion evidence / remaining decision | Safest supervisor action |
|---|---|---|---|
| `audit-firstmate-backlog-r1` | Read-only audit of backlog, cleanup decisions, and existing-PR guard. | **[R]** `data/audit-firstmate-backlog-r1/report.md`; terminal receipt `state/audit-firstmate-backlog-r1.status`. One separate captain choice remains: prohibit broad cleanup modes or fund ownership/manifest/quiescence hardening. | Mark complete; preserve the report and captain decision. Do not clean its copy while guard work remains unlanded. |
| `audit-license-expiry-notifications-r1` | Read-only audit of controller/frontend expiry-notification paths. | **[R]** `data/audit-license-expiry-notifications-r1/report.md`; `state/audit-license-expiry-notifications-r1.status`. Findings include Telegram-only outbound delivery, Portal envelope mismatch, process-local deduplication, and disabled-channel false success. | Close as delivered. Fixes require separately authorized tasks. Do not clean shared nextzeroctl copy 1 blindly. |
| `audit-nextzero-agent-src-watchdog-modularity-r1` | Behavior-preserving modularity audit and phased plan; no implementation. | **[R]** `data/audit-nextzero-agent-src-watchdog-modularity-r1/report.md`, `report.html`, `desktop-qa.png`; completion in `state/audit-nextzero-agent-src-watchdog-modularity-r1.status`. | Preserve artifacts and mark complete. Any correctness/refactor work needs separate scope and platform validation authority. |
| `debug-headscale-v0292-noise-handshake-r1` | Read-only Noise-handshake diagnosis without production mutation. | **[R]** `data/debug-headscale-v0292-noise-handshake-r1/report.md` and `report.html`. The report correctly preserves the original task ID while correcting the affected version to **0.28.0**, reproducing the decrypt failure, and showing later hijacked-response warnings are secondary. Follow-up choice remains: identity/routing audit, response-ownership fix, both, or defer. | Accept the report as complete. Carry the follow-up as a separate captain-held choice; do not restart the scout or rotate keys/reset nodes/change production state. |
| `dlp-control-identity-contract-alignment-g1` | Research owner boundaries for DeviceIdentity and DLP assignment/control contracts. | **[R]** `data/dlp-control-identity-contract-alignment-g1/report.md`; terminal receipt `state/dlp-control-identity-contract-alignment-g1.status`. Owner-published contracts must precede endpoint G2. | Close as delivered and retain dependency ordering. Do not infer implementation authority or clean shared nextzeroctl copy 1. |
| `research-posture-realtime-rule-stats-r1` | Recommend MongoDB-versus-Redis design and create one tracking issue. | **[R]** `data/research-posture-realtime-rule-stats-r1/report.md`; `state/research-posture-realtime-rule-stats-r1.status`; issue [812](https://github.com/nextzeroinc/nextzeroctl/issues/812). Captain still must choose fleet population, offline/stale semantics, and freshness promise. | Close the research task. Keep `implement-posture-current-rule-stats-r1` as the sole decision item; do not clean shared copy 3. |
| `scope-pr11-partial-product-key-r1` | Extend PR11 study with UI behavior, PR12 consistency, and exact controller predicates; no implementation. | **[R]** `data/scope-pr11-partial-product-key-r1/report.md`, `report.html`; completion in `state/scope-pr11-partial-product-key-r1.status`. Report supports policy-only and policy-state-AND-suffix modes; OR remains excluded. | Mark complete and preserve artifacts. Any cross-repository implementation requires separate authorization; do not clean shared copy 1 blindly. |

## 7. Merged or synchronized work needing safe disposition

| Task | Authorized deliverable | Completion evidence and uncertainty | Safest supervisor action |
|---|---|---|---|
| `dlp-baseline-evidence-refresh` | Refresh DLP architecture/endpoint evidence inventory and deliver a PR. | **[R]** `data/dlp-baseline-evidence-refresh/contributions.json` records architecture PR [23](https://github.com/nextzeroinc/nextzero-architecture/pull/23) merged at `cb192276…` with validation successful. **[M]** Captain approved/merged it and the primary copy was synced to `e473b14`. `data/backlog.md` still saying “awaits Captain review” is stale. | Reconcile supervisory/backlog truth to merged. Clean only after proving the dedicated copy has no unique dirty content. |
| `dlp-contract-executable-conformance-c2` | Replay 42 contract fixtures through Rust APIs with mutation sensitivity. | **[R]** `data/dlp-contract-executable-conformance-c2/contributions.json` records endpoint PR [10](https://github.com/nextzeroinc/dlp-endpoint/pull/10) merged at `0fe9a18…` with Linux/macOS/Windows checks green. **[M]** Retained copy is dirty. | Mark merged, but inventory and preserve the dirty delta before any reset or teardown. |
| `dlp-vi-en-profile-corpus` | Held-out VI/EN corpus, metrics, bounds, tests, and documentation. | **[R]** `data/dlp-vi-en-profile-corpus/contributions.json` records endpoint PR [9](https://github.com/nextzeroinc/dlp-endpoint/pull/9) merged at `8db1e099…` with three platform checks green. **[M]** Retained copy is dirty. Historical commit/current local branch observations differ across records. | Using the exact path in `state/dlp-vi-en-profile-corpus.meta`, separately record the historical implementation commit, current branch/worktree head, merged PR head, and dirty delta. Preserve all differences before cleanup; never force-reset merely because the PR merged. |
| `sync-kb-main-r1` | Discard only the authorized transient `log.md` edit and synchronize Knowledge Base main. | **[R]** `state/sync-kb-main-r1.status` records successful synchronization to `f3442e89486f43e1395ab54b6a2547e076d1e6d4`. The primary managed clone later appears to have advanced, so that does not invalidate the historical task. Current cleanliness is unproven. | Treat the maintenance task as complete. Distinguish the exact worktree in `state/sync-kb-main-r1.meta` from the primary managed clone, record both current heads, and do not reset either to the historical SHA. Teardown only after a clean/ownership check. |

## Supervisor priority

1. Leave the live CLI worker untouched.
2. Recover DLP wire custody with `no-mistakes axi sync --recover`, preserving both heads and resolving only the current Windows privacy finding.
3. Review PR 256’s existing head and accepted scope; use its live green checks rather than inventing another gate.
4. Request normal captain approval for PR 256 and fresh explicit merge confirmation for PR 809.
5. Recover the existing-PR guard only in a fresh isolated copy after proving approved-fork write access.
6. Recover the missing PR 716 output before considering a fresh read-only review.
7. Preserve every dirty or shared copy; completion or merge alone does not make cleanup safe.