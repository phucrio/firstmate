# Reset recovery matrix

**Evidence legend:** **[R]** independently verified from local repository/task records; **[M]** supplied by the main supervisor from live endpoint/forge inspection. Status files are treated as historical corroboration, not current liveness.

## 1. Active continuation

| Task | Authorized deliverable | Completion evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `diagnose-nextzero-cli-v113-beta1-docker-login-r1` | Narrow state-aware authentication start for fresh Docker enrollment; preserve enrolled-node reconnect behavior; tests and PR to `stg`. | **[R]** Ship contract: `data/diagnose-nextzero-cli-v113-beta1-docker-login-r1/ship-instructions.md`. Historical status records commit `2098801`, successful local tests, and subsequent accepted sentinel corrections: `state/diagnose-nextzero-cli-v113-beta1-docker-login-r1.status`. **[M]** This is the sole active worker, recently relaunched in its original isolated copy. | Implementation/validation is actively changing; no stable final head or PR may yet be asserted. No unresolved product choice was supplied. | **Leave it alone and let the live worker continue.** Do not inspect its implementation, relaunch it, or treat the historical commit as final. |

## 2. Restart-needed implementation

| Task | Authorized deliverable | Completion evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `dlp-browser-wire-contract-f1` | Endpoint-owned strict Chrome-first browser/native-host wire schema, generated Rust/TypeScript consumers, 19-vector executable gate, bounded simulated host/PEP profile; no real-browser qualification. | **[R]** Scope: `data/dlp-browser-wire-contract-f1/brief.md`. Original isolated copy still exists at the path in `state/dlp-browser-wire-contract-f1.meta`; its branch is `fm/dlp-browser-wire-contract-f1` at local submitted head `06136785e02578a63ca144b1dcbb3c9ee097fec4`. Historical local implementation/validation completion appears in `state/dlp-browser-wire-contract-f1.status`. | **Not delivered.** **[M]** Authoritative run `01M2JH57W97ZTWYQD9D5R5R4W1` failed because the daemon shut down; pipeline-owned unpublished head is `704165985b38c3a268cc97e5dfe3800783051c6c`. **[R]** The last durable gate remains unresolved: Windows runtime-state privacy/ACL enforcement versus rejecting Windows durable state, in `data/dlp-browser-wire-contract-f1/nm-01M2JH57W97ZTWYQD9D5R5R4W1-fix24-findings.txt`. Earlier “chọn A/B” decisions do not explicitly answer this later Windows finding. | **Priority continuation:** in the recorded original copy, run exactly `no-mistakes axi sync --recover`; do not reset/rebase or edit first. Confirm recovered custody preserves unpublished head `704165…`. Obtain an explicit answer to the fix24 Windows ACL/rejection choice, feed it to the recovered gate, and continue the same guarded delivery to a green PR. Do not merge. |

## 3. Green PRs waiting approval/review

| Task | Authorized deliverable | Completion evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `nextzero-pkgs-telegram-release-alert-r1` | Admin-triggered Telegram release alert plus configurable bilingual template, explicit confirmation, durable audit/idempotency, no automatic or real Telegram send; update existing PR 256 on `stg`. | **[R]** Contract: `data/nextzero-pkgs-telegram-release-alert-r1/brief.md`. Recorded branch is at `2eab03b739a255536c214a22b1e7bec38395150c`, matching `state/nextzero-pkgs-telegram-release-alert-r1.meta`. Historical final record says base `stg`, six threads resolved, corrected Markdown body, responsive UI matrix green, stale-send recovery implemented, and no Telegram contacted: `state/nextzero-pkgs-telegram-release-alert-r1.status`. **[M]** PR https://github.com/nextzeroinc/nextzero-pkgs/pull/256 is open with 2 passed, 0 failed, 2 skipped. | Contradiction: the historical status calls run `01M2MZXGVJ5THSQN256NJK6BFH` “checks-passed,” while **[M]** authoritative pipeline state says that run failed during CI with daemon shutdown. The open PR itself is nevertheless currently green. `contributions.json` could not observe the forge, so it does not independently resolve this. Presence of the requested ztrust-lm-fe visual-review follow-up issue is not evidenced locally. No separate “audit gate” is authorized. | **Priority continuation:** do not restart implementation or rerun an obsolete cancelled run merely to manufacture another gate. Review the existing PR at head `2eab03b…` against the accepted scope: base `stg`, resolved threads, body/UI evidence, optional hashtags, stale-send recovery, stored-secret readiness, no real send, and requested follow-up issue. If those existing review obligations are satisfied, present this already-green PR for normal captain approval/merge; otherwise make only the missing in-scope correction on the same branch/PR and revalidate once. |
| `fix-nextzeroctl-pr809-codex-r1` | Resolve five Codex findings on existing nextzeroctl PR 809 without changing its release-flow semantics. | **[R]** Branch head is `d5a537e0baaefc53bb0f66fbfdb92813a79727eb`, matching `state/fix-nextzeroctl-pr809-codex-r1.meta`; historical completion says all five threads resolved and five checks green: `state/fix-nextzeroctl-pr809-codex-r1.status`. **[M]** https://github.com/nextzeroinc/nextzeroctl/pull/809 remains open with five passing checks. | Explicit captain hold remains current: merging triggers the Enterprise main/SHA deployment and release flow; merge must be a merge commit, not squash/rebase. Fresh merge confirmation is required. | Keep open and unchanged. Request **fresh explicit captain merge confirmation**; if granted, use the documented merge-commit release path. Do not infer approval from earlier implementation authorization. |

## 4. Ownership-blocked or artifact-uncertain work

| Task | Authorized deliverable | Completion evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `guard-existing-pr-pipeline-r1` | Fail-closed Firstmate path that proves an explicitly supplied existing PR’s repo/base/head and prevents fallback to a generated `fm/<task>` replacement PR. | **[R]** Contract: `data/guard-existing-pr-pipeline-r1/brief.md`. Preserved branch/ref exists in `.git/packed-refs` at `059eee4617fd4f5c2b0203cee376a4b9945939a9`; detailed implementation/file/test evidence is in `data/audit-firstmate-backlog-r1/report.md`. | Not shipped. Historical push failed HTTP 403 to `kunchenguid/firstmate`; captain later approved fork delivery. The recorded worktree is shared with `audit-firstmate-backlog-r1` and is now detached at another head, so it is not safe task custody. | Do not use or clean the shared copy. First prove/create a dedicated isolated copy, recover the preserved ref `059eee…`, reconcile it onto current main, rerun focused tests, and deliver through the captain-approved fork. Verify actual fork write permission before starting no-mistakes. |
| `review-pr716-stg-r2` | Read-only two-stage review of nextzeroctl PR 716 against current `stg`, with evidence-backed findings and verdict. | **[R]** Only `data/review-pr716-stg-r2/brief.md` and `state/review-pr716-stg-r2.meta` exist. There is no report or status artifact. **[M]** Main classifies it among completed scouts, but its endpoint is absent. | Repository evidence contradicts the “completed” label: the requested review deliverable cannot be verified. Its recorded copy is shared with `research-posture-realtime-rule-stats-r1`. | Do not blindly relaunch in the shared copy. First recover any missing report/verdict from preserved task transcript/history. If none exists and the review is still useful, authorize a fresh read-only review in a new isolated copy; otherwise record the artifact loss explicitly. |

## 5. Completed reports — preserve decisions, no implementation implied

| Task | Authorized deliverable | Verified evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `audit-firstmate-backlog-r1` | Read-only reality audit of stale backlog/cleanup decisions and existing-PR guard. | **[R]** Complete report at `data/audit-firstmate-backlog-r1/report.md`; completion in `state/audit-firstmate-backlog-r1.status`. It verifies guard head `059eee…`, recommends retaining it, and closes five stale cleanup questions without authorizing deletion. | One genuine captain choice remains outside report completion: prohibit broad cleanup modes, or fund ownership/manifest/quiescence/fail-closed hardening. | Mark the report complete. Preserve the narrowed cleanup captain decision. Do not clean its shared Firstmate copy while guard work remains unlanded. |
| `audit-license-expiry-notifications-r1` | Read-only cross-repo audit of license-expiry delivery channels and gaps. | **[R]** `data/audit-license-expiry-notifications-r1/report.md`; completion in its status file. It verifies Telegram is the only outbound expiry channel, Portal envelope mismatch, process-local dedup, and disabled-channel false success. | No task-level decision. Findings are recommendations, not implementation authority. Copy is shared with two other completed nextzeroctl scouts. | Close the report task; retain it as planning evidence. Do not launch fixes without a separately authorized task and do not clean the shared copy blindly. |
| `audit-nextzero-agent-src-watchdog-modularity-r1` | Read-only modularity/lifecycle audit with Markdown, HTML and visual QA. | **[R]** `data/audit-nextzero-agent-src-watchdog-modularity-r1/report.md`, `report.html`, `desktop-qa.png`; status records eight findings and completed authorized fallback QA. | No unresolved report decision. Correctness proposals—including supervisor ownership and Linux stop-hook reconciliation—remain unauthorized implementation. | Mark complete and preserve artifacts. Any implementation should be separately scoped, beginning with concurrency characterization rather than a wholesale rewrite. |
| `debug-headscale-v0292-noise-handshake-r1` | Diagnose reported Noise handshake failures and deliver evidence/mitigations without production mutation. | **[R]** Corrected authoritative report is `data/debug-headscale-v0292-noise-handshake-r1/report.md` plus `report.html`. It explicitly preserves the task identity while correcting affected scope to deployed **v0.28.0**, reproduces the exact AEAD failure and secondary hijacked-response warnings, and records passing bounded tests. | Historical `needs-decision` offers A read-only identity/routing audit, B response-ownership fix, C both, D defer. That is a **follow-up product choice**, not a reason to call the diagnosis report incomplete. No production cause is proven. | Mark the report complete despite stale backlog “in flight” wording. Preserve the follow-up choice separately; recommended next work is the read-only identity/routing correlation audit. Do not authorize key rotation, node reset, state wipe, or production changes from this report. |
| `dlp-control-identity-contract-alignment-g1` | Research exact owner boundaries for shared DeviceIdentity, controller DLP assignment, and generated endpoint consumption. | **[R]** Complete report: `data/dlp-control-identity-contract-alignment-g1/report.md`; completion in its status. It concludes owner-published contracts must precede endpoint G2. | No implementation was authorized. Architecture algorithm/encoding/provider and policy-profile choices remain dependencies handled by follow-up tasks. Copy is shared with other nextzeroctl scouts. | Close the research task; retain its dependency ordering and owner boundaries. Do not treat report completion as permission to implement G2 or copy schemas into the endpoint. |
| `research-posture-realtime-rule-stats-r1` | Recommend durable current per-rule posture statistics architecture and create one tracking issue. | **[R]** `data/research-posture-realtime-rule-stats-r1/report.md` documents MongoDB-first latest-evaluation envelopes, Redis only as optional measured response cache, and issue https://github.com/nextzeroinc/nextzeroctl/issues/812. | Current follow-up captain decision is explicit in `data/backlog.md`: managed-fleet population, offline-as-stale semantics, and best-effort 5s/15s projection promise versus alternatives. The report itself is complete. | Close the research task while retaining `implement-posture-current-rule-stats-r1` as the sole captain-held implementation decision. Do not clean/relaunch the shared copy with `review-pr716-stg-r2`. |
| `scope-pr11-partial-product-key-r1` | Extend PR11 study with admin UX, PR11/PR12 consistency and exact controller predicates; no implementation. | **[R]** `data/scope-pr11-partial-product-key-r1/report.md` and `report.html`; status records revised two-mode report and visual QA. The report preserves policy-only and policy-state-AND-suffix modes and excludes OR. | No unresolved task decision. Implementation remains unauthorized and spans agent, controller and Portal. Copy is shared with other completed nextzeroctl scouts. | Mark complete; retain as product/implementation planning evidence. Do not infer approval to implement or clean the shared copy. |

## 6. Completed merged/synced work — safe cleanup only after preservation checks

| Task | Authorized deliverable | Verified evidence | Remaining work / decision | Safest supervisor action |
|---|---|---|---|---|
| `dlp-baseline-evidence-refresh` | Documentation-only refresh of DLP architecture evidence and immutable endpoint/browser baselines. | **[R]** `data/dlp-baseline-evidence-refresh/contributions.json` observes https://github.com/nextzeroinc/nextzero-architecture/pull/23 **merged** at `cb192276…` with `validate` successful. Local branch points to that head. **[M]** Primary copy is synced to `e473b14`. | Contradiction: `data/backlog.md` still says PR 23 awaits captain review; that entry is stale. | Mark merged/complete and update supervisory truth. Before teardown, verify no unique dirty files; then clean only this dedicated copy. |
| `dlp-contract-executable-conformance-c2` | Replay all endpoint contract fixtures through Rust APIs with mutation sensitivity. | **[R]** `data/dlp-contract-executable-conformance-c2/contributions.json` observes https://github.com/nextzeroinc/dlp-endpoint/pull/10 merged at `0fe9a18d…` with Linux/Windows/macOS checks successful; local branch is at the same merged head. | **[M]** Preserved isolated copy contains uncommitted changes. No product decision remains. | Mark merged/complete, but **do not discard or reset the copy**. Inventory and preserve the dirty delta first; clean only after proving it is redundant or separately retained. |
| `dlp-vi-en-profile-corpus` | Held-out VI/EN corpus, metrics, bounds, tests and documentation. | **[R]** `data/dlp-vi-en-profile-corpus/contributions.json` observes https://github.com/nextzeroinc/dlp-endpoint/pull/9 merged at `8db1e099…` with three platform checks successful. | Local task branch currently points to `f2ec7af…`, not the merged head, and **[M]** the copy has uncommitted changes. | Mark merged/complete. Preserve both the branch state and dirty delta before any teardown; do not force-reset it merely because the PR merged. |
| `sync-kb-main-r1` | Sync the Knowledge Base main checkout and remove only the authorized transient `log.md` change. | **[R]** `state/sync-kb-main-r1.status` records completion at `f3442e89486f43e1395ab54b6a2547e076d1e6d4`; the recorded worktree HEAD independently matches that SHA. | No unresolved decision. Current cleanliness was not independently derivable with read-only file tools. | Treat sync as complete. Verify cleanliness/ownership before normal teardown; preserve anything unexpected. |

## Supervisor priority order

1. **Do not touch the active CLI worker.**
2. **Recover DLP wire custody with `no-mistakes axi sync --recover`**, preserve `704165…`, and resolve the Windows private-directory finding.
3. **Review existing package PR 256 at `2eab03b…`**, reconciling the failed pipeline record with the currently green forge checks; do not invent another serial gate.
4. Seek captain approval for the two green PRs: normal approval for package PR 256 and **fresh explicit merge confirmation** for held nextzeroctl PR 809.
5. Recover the existing-PR guard from `059eee…` only in a newly proven dedicated copy and through the approved fork.
6. Close completed reports/merged work administratively, but postpone cleanup wherever copies are shared or dirty.